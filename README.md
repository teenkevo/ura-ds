# URA Data Science Portfolio (Mono-repo)

## Projects
- `projects/wht-exemption/` — Withholding Tax Exemption AI Model

## Development
- Python project uses `pyproject.toml`
- CI runs lint + tests on PRs
- Branch protection should require CI checks and CODEOWNERS approval

## Documentation
See `/docs` and each project’s `MODEL_CARD.md`.
